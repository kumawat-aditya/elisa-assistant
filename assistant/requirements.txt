
# Audio and speech
PyAudio==0.2.14
simpleaudio==1.0.4
webrtcvad==2.0.10
numpy==1.26.4

# Wake word detection
openwakeword==0.6.0

# WebSocket UI
websockets==15.0.1

# HTTP requests
requests==2.32.5

# Misc
threadpoolctl==3.6.0  # Used by some dependencies

# Remove unused packages (onnxruntime, tflite-runtime, scikit-learn, scipy, sympy, protobuf, flatbuffers, coloredlogs, tqdm, packaging, joblib, mpmath, humanfriendly, certifi, charset-normalizer, idna, urllib3)
